<Project>

  <!--
    Single source of truth for cross-project MSBuild settings.

    The <Version> property is read by .github/workflows/auto-tag.yml on
    every push to master; if the parsed value has no matching v* tag the
    workflow creates one and triggers release.yml. After publishing,
    release.yml updates repo.json's version and release metadata.

    The version MUST have four parts (Major.Minor.Build.Revision). The SDK
    stamps the shipped manifest from the assembly version, which .NET pads
    to four parts.
  -->

  <PropertyGroup Label="Identity">
    <Version>2.9.1.0</Version>
    <Authors>XeldarAlz</Authors>
    <Copyright>Copyright (c) XeldarAlz</Copyright>
    <PackageProjectUrl>https://github.com/XeldarAlz/FFXIV-AutoFATEGrind</PackageProjectUrl>
    <PackageLicenseExpression>AGPL-3.0-or-later</PackageLicenseExpression>
    <RepositoryUrl>https://github.com/XeldarAlz/FFXIV-AutoFATEGrind</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
  </PropertyGroup>

  <PropertyGroup Label="Determinism" Condition="'$(GITHUB_ACTIONS)' == 'true'">
    <ContinuousIntegrationBuild>true</ContinuousIntegrationBuild>
    <Deterministic>true</Deterministic>
  </PropertyGroup>

</Project>
