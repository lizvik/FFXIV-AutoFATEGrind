name: Auto-tag on version bump

# Whenever master moves, read the plugin version from Directory.Build.props.
# If that version has no matching v* tag yet, create one and push it, then
# dispatch release.yml explicitly to publish latest.zip.
#
# Bump <Version> in Directory.Build.props before (or inside) your PR and
# you get a release automatically once it merges. Commit without a version
# bump and nothing happens. release.yml updates repo.json after publishing.
#
# Tag pushes made with GITHUB_TOKEN do not cascade-trigger other workflows.
# workflow_dispatch is exempt from that restriction, so this workflow invokes
# release.yml directly and does not require a personal access token.

on:
  push:
    branches:
      - master

concurrency:
  group: auto-tag-master
  cancel-in-progress: false

permissions:
  actions: write
  contents: write

jobs:
  tag:
    runs-on: ubuntu-latest
    if: "!contains(toJson(github.event.commits.*.message), '[skip auto-tag]')"

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Read plugin version from Directory.Build.props
        id: version
        run: |
          version=$(grep -oP '<Version>\K[\d.]+(?=</Version>)' Directory.Build.props)
          if [ -z "$version" ]; then
            echo "::error::Could not parse <Version> from Directory.Build.props"
            exit 1
          fi
          if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::<Version> ($version) must have four parts (Major.Minor.Build.Revision); a three-part version ships a manifest Dalamud rejects."
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=v$version" >> "$GITHUB_OUTPUT"
          echo "Plugin version: $version"

      - name: Check whether tag already exists
        id: check
        run: |
          tag="${{ steps.version.outputs.tag }}"
          if git rev-parse -q --verify "refs/tags/$tag" >/dev/null; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Tag $tag already exists — nothing to do."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "Tag $tag does not exist yet — will create."
          fi

      - name: Create and push tag
        if: steps.check.outputs.exists == 'false'
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "$TAG" -m "Release $TAG"
          if git push origin "$TAG"; then
            echo "Pushed tag $TAG."
          elif git ls-remote --tags origin "refs/tags/$TAG" | awk '{print $2}' | grep -qx "refs/tags/$TAG"; then
            echo "Tag $TAG was created by another run — nothing to do."
          else
            echo "::error::Failed to push tag $TAG"
            exit 1
          fi

      - name: Check whether release already exists
        id: release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          if gh api "repos/${{ github.repository }}/releases/tags/$TAG" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Release $TAG already exists — nothing to do."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "Release $TAG does not exist yet — will dispatch release.yml."
          fi

      - name: Dispatch release workflow
        if: steps.release.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.version.outputs.tag }}
        run: gh workflow run release.yml --ref master --field tag="$TAG"
